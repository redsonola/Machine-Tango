//
//  NotchData.h
//  Machine Tango
//
//  Created by courtney on 3/5/19.
//

#ifndef NotchData_h
#define NotchData_h

class NotchData : public ShimmerData
{
/*
    msg.add(bone.getName());
    
    if (acc != null)
        for (int j = 0; j < 3; j++)
            msg.add(acc.get(j));
            
            if (pos != null)
                for (int j = 0; j < 3; j++)
                    msg.add(pos.get(j));};
 
 //matching previous setup -- LeftHeel, RightHeel, either [ChestTop, ChestBottom]
 
 So, actually one notch message is equivalent to many shimmer messages -- could break up the message?...or not

 */
    
};

#endif /* NotchData_h */
